{
  "name": "linkedin-sequence-generator-backend",
  "version": "1.0.0",
  "description": "Backend API for LinkedIn prospect messaging sequence generation",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/db/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "db:reset": "node -e \"const fs=require('fs'); const f='./data/sqlite.db'; [f,f+'-wal',f+'-shm',f+'-journal'].forEach(p=>{try{fs.rmSync(p,{force:true})}catch(e){if(e.code==='EPERM'){console.error('ERROR: Stop the dev server first (the DB file is locked).'); process.exit(1)}; throw e}}); console.log('Database deleted.')\" && npm run db:migrate"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.38.0",
    "express": "^4.21.0",
    "nanoid": "^5.0.9",
    "openai": "^4.77.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.25",
    "@types/node": "^22.10.0",
    "drizzle-kit": "^0.30.0",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
